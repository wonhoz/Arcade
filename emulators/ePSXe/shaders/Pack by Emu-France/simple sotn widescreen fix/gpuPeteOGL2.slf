// SotN Widescreenfix 
// only crop SotN to widescreen
// This shuld only a template for other SotN-Shaders.
// The Fragmentshader (this file) doe  no changes.
// Only Vertexshader changes the hight of the output.


uniform sampler2D OGL2Texture;

void main()
{
 gl_FragColor = texture2DProj(OGL2Texture, gl_TexCoord[0]);
}